apiVersion: apps/v1
kind: Deployment
metadata:
  name: jvtheque-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jvtheque-db
  template:
    metadata:
      labels:
        app: jvtheque-db
    spec:
      containers:
        - name: postgres
          image: postgres:17.5-alpine3.22
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: jvtheque_db
            - name: POSTGRES_USER
              value: jvtheque_user
            - name: POSTGRES_PASSWORD
              value: jvtheque_password
          volumeMounts:
            - name: postgres-storage
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-storage
          persistentVolumeClaim:
            claimName: jvtheque-db-pvc 
